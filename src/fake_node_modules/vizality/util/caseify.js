const convert = require('js-convert-case');

module.exports = (type, str) => {
  switch (type) {
    case 'camel':
      return convert.toCamelCase(str);
    case 'pascal':
      return convert.toPascalCase(str);
    case 'snake':
      return convert.toSnakeCase(str);
    case 'dot':
      return convert.toDotCase(str);
    case 'kebab':
    case 'param':
    case 'slug':
      return str.toString().toLowerCase().trim()
        .replace(/\s|_+/g, '-') // Replace spaces and underscores with -
        .replace(/&/g, 'and') // Replace & with and
        .replace(/[^\w-]+/g, '') // Remove all non-word chars
        .replace(/--+/g, '-'); // Replace multiple - with single
    case 'path':
      return convert.toPathCase(str);
    case 'sentence':
      return convert.toSentenceCase(str);
    case 'header':
      return convert.toHeaderCase(str);
  }
};
// Has anyone made a game with Electron and published on Steam? I_like the -cool ---really cool stuff
