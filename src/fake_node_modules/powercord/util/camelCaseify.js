/*
 * module.exports = (str) =>
 *   str
 *     .split('-')
 *     .map((s, i) => (
 *       i === 0
 *         ? s
 *         : s[0].toUpperCase() + s.slice(1)
 *     ))
 *     .join('');
 */

module.exports = (str) =>
  str
    .replace(/(?:^\w|[A-Z]|\b\w)/g, (match, index) =>
      index === 0 ? match.toLowerCase() : match.toUpperCase())
    .replace(/\s+|&/g, '');
