const { React, getModuleByDisplayName } = require('powercord/webpack');
const AsyncComponent = require('../AsyncComponent');
const FormItem = require('./FormItem.jsx');

const Region = AsyncComponent.from(getModuleByDisplayName('RegionSelector'));

module.exports = class RegionSelector extends React.Component {
  render () {
    const { children: title, note, required } = this.props;
    delete this.props.children;

    return (
      <FormItem title={title} note={note} required={required}>
        <Region {...this.props}/>
      </FormItem>
    );
  }
};

/**
 * AVAILABLE PROPS
 *
 * disabled={ false }
 * error={ false }
 * onClick={ '' }
 * region={[
 *   {
 *     custom: false,
 *     deprecated: false,
 *     id: 'us-south',
 *     name: 'US South',
 *     optimal: true,
 *     vip: false
 *   }
 * ]}
 */
