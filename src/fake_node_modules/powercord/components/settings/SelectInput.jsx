const { React, getModuleByDisplayName } = require('powercord/webpack');
const AsyncComponent = require('../AsyncComponent');
const FormItem = require('./FormItem.jsx');

const Select = AsyncComponent.from(getModuleByDisplayName('SelectTempWrapper'));

module.exports = class SelectInput extends React.Component {
  render () {
    const { children: title, note, required } = this.props;
    delete this.props.children;

    return (
      <FormItem title={title} note={note} required={required}>
        <Select {...this.props}/>
      </FormItem>
    );
  }
};

/**
 * AVAILABLE PROPS
 *
 * className: "marginBottom20-32qID7"
 * clearable={ false }
 * onChange={ '' }
 * options={[
 *   {value: "NO_INSTANT_INVITE", label: "No Invite"}
 *   {value: "703488974949187595", label: "Text Channels"}
 *   {value: "703488974949187596", label: "Voice Channels"}
 *   {value: "703488974949187597", label: "#general"}
 *   {value: "703488974949187598", label: "General"}
 * ]}
 * searchable={ false }
 * value={ value: "NO_INSTANT_INVITE", label: "No Invite" }
 */
