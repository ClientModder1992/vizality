const { getModuleByDisplayName, React } = require('powercord/webpack');
const AsyncComponent = require('../AsyncComponent');
const FormItem = require('./FormItem.jsx');

const Picker = AsyncComponent.from(getModuleByDisplayName('ColorPicker'));

module.exports = class ColorPicker extends React.Component {
  render () {
    const { children: title, note, required } = this.props;
    delete this.props.children;

    return (
      <FormItem title={title} note={note} required={required}>
        <Picker {...this.props}/>
      </FormItem>
    );
  }
};

/**
 * AVAILABLE PROPS
 *
 * colors={[
 *   1752220, 3066993, 3447003, 10181046, 15277667, 15844367, 15105570, 15158332, 9807270, 6323595, 1146986, 2067276, 2123412, 7419530, 11342935, 12745742, 11027200, 10038562, 9936031, 5533306
 * ]}
 * defaultColor={ 10070709 }
 * disabled={ false }
 * onChange={ '' }
 * value={ 0 }
 */
