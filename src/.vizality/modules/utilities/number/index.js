/**
 * @module util.number
 * @namespace util.number
 * @memberof util
 * @version 0.0.1
 */

const number = {
  isNumber (input) {
    return void 0 || input;
  },

  assertNumber (input) {
    return void 0 || input;
  }
};

module.exports = number;
