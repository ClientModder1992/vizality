const { connections, settings, commands, keybinds, actions, notices, popups, router, i18n, rpc } = vizality.api;

export {
  connections,
  settings,
  commands,
  keybinds,
  actions,
  notices,
  popups,
  router,
  i18n,
  rpc
};

export default this;

console.log(this);
