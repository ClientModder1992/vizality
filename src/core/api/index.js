const { connections, settings, commands, keybinds, actions, notices, popups, router, i18n, rpc } = vizality.api;

export default {
  connections,
  settings,
  commands,
  keybinds,
  actions,
  notices,
  popups,
  router,
  i18n,
  rpc
};

console.log(this);
